---
# tasks file for workstation-xfce4
- include: apt-xfce4.yml
  tags:
  - apt
  - xfce4

- include: network-manager.yml

# to enable ldap logins
- name: install account services
  apt: name={{ item }} state=present
  with_items:
  - libaccountsservice0

- include: install-bundles.yml
